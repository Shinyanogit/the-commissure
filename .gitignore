# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Xcode and Swift build output
DerivedData
.build
xcuserdata
*.xcuserstate

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Blender source files — too large for GitHub (some >100MB), shared externally. See README.
blender/
*.blend
*.blend1

# Internal working docs are local by default. The product/design contract and
# native iOS specifications below are intentionally versioned for collaborators.
docs/*
!docs/DESIGN_CONCEPT.md
!docs/ios/
!docs/ios/**

# Vercel CLI local link (contains project/org ids) — set as GitHub secrets instead
.vercel

# Personal AI settings and handoff notes — never shared
.claude/
FOR*.md
.env*
*.p8
*.p12
*.mobileprovision
