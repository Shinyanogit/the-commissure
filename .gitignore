# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Blender source files — too large for GitHub (some >100MB), shared externally. See README.
blender/
*.blend
*.blend1

# Internal working docs (project spec / TODO) — kept local, not shared on GitHub
docs/

# Vercel CLI local link (contains project/org ids) — set as GitHub secrets instead
.vercel

# Claude Code personal/local settings — never shared
.claude/settings.local.json
.env*
