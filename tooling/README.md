# Tooling

This directory owns deterministic validators, asset conversion helpers, pack
construction, and release checks shared by CI.

Runtime application code and private signing credentials do not belong here.
